# Ledger Light Admin (Monorepo)

Monorepo structure:
- `backend/` Node API
- `frontend/` (reserved)

## Prereqs
- Docker + Docker Compose

## Quick start (one command)
```bash
cp .env.example .env
docker compose up --build
```